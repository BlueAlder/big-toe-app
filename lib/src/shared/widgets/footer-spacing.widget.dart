import 'package:flutter/material.dart';

class FooterSpacing extends StatelessWidget {
  const FooterSpacing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 30);
  }
}
